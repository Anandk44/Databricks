# This is a Declarative Automation Bundle definition for bakehouse_analytics.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: bakehouse_analytics
  uuid: ece2326c-9147-4fbb-89d0-159368380e49

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    # mode: development
    presets:
      name_prefix: dev_
    default: true
    workspace:
      host: https://dbc-8d29592a-8e37.cloud.databricks.com
    variables:
      catalog: bakehouse_analytics_dev
      schema: dev
  prod:
    mode: production
    presets:
      name_prefix: dev_
    workspace:
      host: https://dbc-8d29592a-8e37.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/anandkoyalmudi1000@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/anandkoyalmudi1000@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: bakehouse_analytics_dev
      schema: prod
    permissions:
      - user_name: anandkoyalmudi1000@gmail.com
        level: CAN_MANAGE
